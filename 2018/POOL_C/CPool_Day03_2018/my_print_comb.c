/*
** EPITECH PROJECT, 2018
** my_print_comb
** File description:
** 3 different digits make ascending list
*/

void my_putchar(char c);

void print(char a, char b, char c)
{
    while (c <= 9)
        my_putchar (a);
        my_putchar (b);
        my_putchar (c);
            if (a != 7)
                my_putchar (',');
                my_putchar (' ');
                {
c++
    }
    
}    

int my_print_comb(void)
{
    char a;
    char b;
    char c;

    a = '0';
    {
        while (a <= '7')
            b = a + 1
            c = b + 1
             while (b <= '8')
             {
                 print (a, b, c)
                 c = b + 1
                 b++
                }
                a++
                    }
    {
        return (0)
            }
        
}
